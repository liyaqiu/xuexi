server:
  port: 8080
logging:
  config: classpath:log4j2.xml

spring:
  resources:
    static-locations:
      - classpath:/abc/
      - classpath:/static/

#  servlet:  #文件上传设置
#    multipart:
#      max-file-size: 10MB
#      max-request-size: 10MB

myconfig:
  path: /root/nginx
  name: liyaqiu-yaml
  age: 31-yaml

